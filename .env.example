# Environment variables for proxy benchmark tests
# Copy this file to .env and fill in your real proxy credentials

# Test proxy credentials (comma-separated list)
# Format: protocol:host:port:username:password:enabled,protocol:host:port:username:password:enabled
TEST_PROXIES=http:proxy.example.com:8080:username:password:enabled,socks:proxy.example.com:1080:username:password:enabled